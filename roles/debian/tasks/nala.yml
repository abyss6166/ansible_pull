- name: download nala script
  copy:
    src: files/nala.sh
    dest: /home/{{ user }}
    owner: "{{ user }}"
    group: "{{ user }}"

- name: add nala to sources list
  tags: nala
  become: true
  shell:
    cmd: ./nala.sh
    chdir: /home/{{ user }}
  args:
    executable: /bin/bash
