- name: install packages
  tags: base
  community.general.xbps:
    name: '{{item}}'
  loop:
      #GUI
      - arc-theme
      - arc-icon-theme
      - dbus
      - kde5
      - kde5-baseapps
      - xorg
      - nano

      #Applications
      - vscode
      - quiterss
      - xdg-utils
      - xdg-user-dirs
      - curl
      - wget
      - dconf
      - onedrive
      - linux-headers
      - pulseaudio
      - linux-headers
      - python3-psutil

      # - xinit
  #  state: latest
